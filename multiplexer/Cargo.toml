[package]
name = "multiplexer"
version = "0.1.0"
authors = ["Ji Park <ji@jipark.dev>"]
edition = "2021"

[[bin]]
path = "src/main.rs"
name = "multiplexer"

[dependencies]
actix-web = "4"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
common = { path = "../common" }
bbs = { path = "../bbs" }
